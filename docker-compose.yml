services:
  maker:
    container_name: maker
    build:
      context: ./exchange
    ports:
      - "5672:5672"
      - "15672:15672"

  prints:
    container_name: prints
    build:
      context: ./exchange
    ports:
      - "3001:3001"
    depends_on:
      - maker
